def solution(arr):
    answer = []
    n = len(arr)
    answer.append(arr[0])
    for i in range (n-1):
        if arr[i] == arr[i+1]:
            continue
        else:
            answer.append(arr[i+1])
    return answer
